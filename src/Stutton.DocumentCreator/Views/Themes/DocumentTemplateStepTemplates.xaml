<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:stepVms="clr-namespace:Stutton.DocumentCreator.ViewModels.Documents.DocumentTemplateSteps"
                    xmlns:mdeControls="clr-namespace:MaterialDesignExtensions.Controls;assembly=MaterialDesignExtensions"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <DataTemplate DataType="{x:Type stepVms:DetailsStepViewModel}">
        <DataTemplate.Resources>
            <Style BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}"
                   TargetType="TextBox" />
        </DataTemplate.Resources>

        <StackPanel>
            <TextBox Text="{Binding Model.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Name"/>
            
            <TextBox Text="{Binding Model.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Description"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Text="{Binding Model.TemplateFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Template Document"/>
                
                <Button Grid.Column="1"
                        Content="BROWSE"
                        Style="{StaticResource MaterialDesignFlatButton}"/>
            </Grid>
            
            <ComboBox Style="{StaticResource MaterialDesignComboBox}"
                      materialDesign:HintAssist.Hint="Associated Work Item Type"/>
            
            <mdeControls:StepButtonBar Continue="CONTINUE" Cancel="CANCEL"/>
            
        </StackPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type stepVms:FieldsStepViewModel}">
        <StackPanel>
            <TextBlock Text="Fields stuff goes here"/>
            <mdeControls:StepButtonBar Back="BACK" Continue="CONTINUE" Cancel="CANCEL"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type stepVms:AutomationsStepViewModel}">
        <StackPanel>
            <TextBlock Text="Automations stuff goes here"/>
            <mdeControls:StepButtonBar Back="BACK" Continue="CONTINUE" Cancel="CANCEL"/>
        </StackPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type stepVms:SummaryStepViewModel}">
        <StackPanel>
            <TextBlock Text="Summary stuff goes here"/>
            <mdeControls:StepButtonBar Back="BACK" Continue="FINISH" Cancel="CANCEL"/>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>